events {}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  # HTTP → redirect naar HTTPS
  server {
    listen 80;
    server_name bon.frericksonline.nl;

    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

    location / {
      return 301 https://$host$request_uri;
    }
  }

  # HTTPS
  server {
    listen 443 ssl;
    server_name bon.frericksonline.nl;
    client_max_body_size 15m;

    ssl_certificate     /etc/letsencrypt/live/bon.frericksonline.nl/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bon.frericksonline.nl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    location /api/ {
      proxy_pass         http://backend:3000/api/;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
      proxy_set_header   X-Real-IP $remote_addr;
      proxy_read_timeout 120s;
    }

    location / {
      proxy_pass         http://frontend:80/;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
    }
  }
}
